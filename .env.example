# Copier ce fichier en ".env" et adapter les valeurs.
# NE JAMAIS committer le fichier .env reel (ajoute a .gitignore).

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=adp_user
DB_PASSWORD=AdpProjet2026!
DB_NAME=trafic_aerien

# Utilise uniquement par la commande `init-db` (creation de la base avec un compte admin).
DB_ADMIN_URL=mysql://root@127.0.0.1:3306
